# Normalmente no hace falta ningún archivo .env en el frontend.
# En Coolify (Runtime Environment Variable), el contenedor nginx usa por defecto:
API_UPSTREAM=http://10.10.0.7:3001

# Solo si la API se publica en otro nombre o puerto sin pasar por el proxy de nginx (se aplica al compilar):
VITE_API_URL=
